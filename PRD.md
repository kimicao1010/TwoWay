# MFA 验证器（macOS）产品需求文档

| 项目 | 内容 |
|---|---|
| 产品名称 | 验证器（Authenticator，暂定名，可替换） |
| 平台 | macOS 原生应用 |
| 文档版本 | v1.0 |
| 状态 | 待评审 |
| 关联设计稿 | Ardot `MFA 验证器 macOS App UI`（fileId 726353735557854） |
| 关联 Demo | `mfa-authenticator/index.html`（可交互原型，视觉/交互 1:1 对应本文档） |

**修订记录**

| 版本 | 日期 | 修订 | 说明 |
|---|---|---|---|
| v1.0 | 2026-09-16 | 首版 | 覆盖核心三屏流程 + 左滑操作交互；交互参数以可交互原型实测值为准 |

---

## 1. 背景与目标

macOS 上缺乏一个轻量、离线、可统一管理 TOTP 二次验证码的工具：现有方案要么是手机端（Google Authenticator 等，切屏成本高），要么是浏览器插件（覆盖面窄、不可控）。

**目标**：做一个对标 Google Authenticator 观感与操作心智的 macOS 原生应用，竖向窄窗、极简深色，验证码一眼可读、一键可复制、一滑可管理。

**成功标准**

| 指标 | 目标 |
|---|---|
| 复制一个验证码的操作步数 | 1 步（单次点按） |
| 从看到验证码到粘贴 | ≤ 2 秒 |
| 冷启动到首屏验证码可见 | ≤ 1 秒 |
| 验证码错误率（相对标准 TOTP 实现） | 0 |

---

## 2. 名词定义

| 术语 | 定义 |
|---|---|
| TOTP | 基于时间的一次性密码（RFC 6238），默认 SHA-1 / 6 位 / 30 秒 |
| 密钥 | 服务商提供的 Base32 共享密钥，如 `JBSWY3DPEHPK3PXP` |
| 倒计时环 | 行末圆形进度环，可视化当前 30 秒周期的剩余时间 |
| 左滑操作 | 在列表行上横向拖拽（鼠标拖动 / 触屏左滑），使行内容左移、露出右侧操作按钮 |

---

## 3. 用户与场景

| 场景 | 触发 | 期望路径 |
|---|---|---|
| 登录某网站需要二验 | 弹出验证码输入框 | 打开 App（或全局快捷键）→ 点按对应行 → 粘贴 |
| 新网站开启二验 | 服务商展示二维码 | 添加账户 → 扫码 / 从图片导入 → 完成 |
| 无摄像头 / 二维码在另一台机器截图 | — | 添加账户 → 手动输入密钥 |
| 换机 / 清理不再用的账户 | — | 列表左滑 → 删除 → 二次确认 |
| 核对某个账户的参数（算法/位数/周期） | 排错 | 左滑 → 详情 |

---

## 4. 本期范围

**P0（本期必须交付）**：验证码列表、点按复制、左滑操作（详情｜删除）、添加账户（扫码 + 手动）、账户详情、删除确认、TOTP 引擎与倒计时、搜索。

**P1（下一迭代）**：触控板双指横滑呼出左滑操作、全局快捷键呼出主窗口、编辑账户、导入导出/备份、剪贴板自动清除。

**P2（探索）**：菜单栏常驻与快速取码、iCloud 同步、生物识别锁定、多主题。

**范围外**：HOTP（计数器型）、多租户/团队共享、非 macOS 平台。

---

## 5. 信息架构与页面清单

| 编号 | 页面 | 类型 | 入口 | 出口 |
|---|---|---|---|---|
| 01 | 验证码列表 | 主页面 | 启动默认 | 点行复制；左滑呼出操作；底部按钮去添加 |
| 02 | 添加账户 · 扫描二维码 | 子页面 | 列表底部「添加账户」/ 分段控件 | 「取消」回列表；分段切手动 |
| 03 | 添加账户 · 手动输入 | 子页面 | 分段控件 | 「取消」回列表；添加成功回列表 |
| 04 | 账户详情 | 子页面 | 左滑操作「详情」 | 「删除」呼出确认弹窗 |
| 05 | 删除确认 | 弹窗（覆盖 04） | 详情页删除按钮 | 取消留在详情；确认后回列表 |

---

## 6. 全局需求

| 编号 | 需求 | 规格 | 优先级 |
|---|---|---|---|
| G-01 | 窗口尺寸 | 400 × 700 pt，竖向单列 | P0 |
| G-02 | 窗口圆角 | 12 px，内容溢出裁切 | P0 |
| G-03 | 标题栏 | 高 52 px，左侧 macOS 交通灯（12 px，间距 8），标题居中 13 px，右侧上下文操作 | P0 |
| G-04 | 原型导航 | （仅原型）窗口外 5 个切换按钮 + `←`/`→` 切屏、`Esc` 回列表 | P0（仅 Demo） |
| G-05 | 缩放策略 | 原始像素 + 等比缩放居中，不响应式重排 | P0 |
| G-06 | 字体 | 中文/UI：Noto Sans SC（对应系统实现可用 PingFang SC）；验证码与密钥：JetBrains Mono（对应 SF Mono） | P0 |
| G-07 | 深色主题 | 仅深色，token 见 §9 | P0 |

---

## 7. 功能需求

### 7.1 FR-01 验证码列表（P0）

**结构**（自上而下）：

| 区块 | 规格 |
|---|---|
| 标题栏 | 见 G-03；右侧为「更多」图标（三点，18 px） |
| 搜索框 | 高 36 px，圆角 9，底色 `#232528`，占位文案「搜索账户或发行方」 |
| 账户列表 | `flex:1`，左右内边距 8、上下 4，行间距 2 |
| 底部栏 | 高 72 px，左侧「共 N 个账户」12 px，右侧「＋ 添加账户」胶囊按钮（高 40，圆角 20，强调色底） |

**账户行**：高 76 px，水平内边距 12，内容列（账户名 15 px SemiBold + 验证码 22 px 等宽、字间距 1.5）+ 悬停复制图标（16 px，默认隐藏）+ 倒计时环（28 px，描边 3）。

**列表态规则**：默认行背景 = 窗口底色；悬停 = `#232528`；复制反馈 = `#2E3237`（见 FR-02）。

**验收标准（AC）**
- [ ] 行高、字号、间距与设计稿逐像素一致
- [ ] 验证码为等宽字体，数字变化时列宽不跳动
- [ ] 列表可滚动（账户多于可视区时），滚动条隐藏

### 7.2 FR-02 行交互：点按复制 + 左滑操作（P0，核心）

**交互模型**：行的一级操作是**复制**，二级操作（详情/删除）通过**左滑**呼出。

| # | 触发 | 行为 | 参数 |
|---|---|---|---|
| R1 | **点按行** | 复制该账户当前验证码（不含空格）到剪贴板；行底色闪烁 `#2E3237`；toast「验证码已复制」 | 底色反馈持续 650 ms |
| R2 | **左滑行** | 行内容左移，露出右侧「详情 \| 删除」两个操作块 | 最大位移 160 px（按钮 72 × 2 + 间距 8 + 右边距 8） |
| R3 | 拖拽释放 | 位移 ≤ −72 px（0.45 × 160）→ 吸附展开并保持；否则回弹归位 | 回弹带 300 ms 缓动 `cubic-bezier(.2,.8,.2,1)` |
| R4 | 位移阈值 | 横向位移 > 8 px 且大于纵向位移才判定为拖拽；否则视为点按 | 防误触 |
| R5 | 已展开的行上**点按** | 收起复位（不复制、不跳转） | — |
| R6 | 拖拽结束 | 浏览器/系统补发的 click 必须被吞掉，不得误触发复制 | 前端实现细节，须回归验证 |
| R7 | 点「详情」 | 进入该账户详情页，行自动复位 | 需先渲染该账户数据 |
| R8 | 点「删除」 | 进入该账户详情页并**自动弹出删除确认** | 260 ms 屏幕切换后弹窗 |
| R9 | 展开其他行 | 当前已展开的行自动收起；切换到非列表屏时全部收起 | 同时最多一行展开 |
| R10 | 拖拽过程中 | 关闭 transform 过渡（跟随手指），释放后恢复；禁止文本选中 | `touch-action: pan-y`，竖向滚动不受影响 |
| R11 | 悬停行 | 底色变 `#232528`，行末浮现复制图标（16 px，强调色） | 复制图标点击同样复制，不冒泡 |

**操作块样式**：高 76 px（与行同高）、宽 72 px、圆角 10、间距 8；「详情」底 `#2E3237` 字 `#E9EBED`，「删除」底 `#E5484D` 字白。行底色必须不透明（`#1B1C1E`），否则操作块会透出。

**验收标准（AC）**
- [ ] 点按 = 复制，全程不发生页面跳转
- [ ] 左滑可拖拽跟手，释放按 R3 吸附/回弹
- [ ] 拖拽后的 click 不触发复制（R6）
- [ ] 右滑（正方向位移）不会展开、也不会导致行错位
- [ ] 仅竖向滑动时列表正常滚动，不触发行位移
- [ ] 同时只有一行处于展开态
- [ ] 复制失败（剪贴板不可用/被拒）时给出「复制失败，请手动复制」toast，不静默失败

### 7.3 FR-03 搜索（P0）

- 输入即过滤，匹配**账户名**或**账户标识**，不区分大小写；
- 无结果时列表为空，底部计数同步为「共 0 个账户」；
- 清空输入恢复全量。

### 7.4 FR-04 添加账户（P0）

**分段控件**（高 34，圆角 9，选中段底 `#43474B`）在「扫描二维码 / 手动输入」间切换，两个状态独立成屏。

**扫描二维码**

| 要素 | 规格 |
|---|---|
| 取景框 | 360 × 340，圆角 14，深色渐变底（`#0F121A → #212433`） |
| 取景括号 | 四角 40 × 40，强调色，内边距 20 |
| 扫描线 | 高 2 px，左右渐变发光，纵向往复动画 3.4 s ease-in-out |
| 辅助操作 | 「从图片文件导入二维码」次级按钮（高 40，描边 `#2E3036`） |
| 引导卡 | 「在哪里找到二维码？」+ 服务商路径示例 |

**手动输入**

| 字段 | 规格 | 校验 |
|---|---|---|
| 账户名称 | 文本输入，高 42，占位「例如：you@example.com」 | 为空时提交使用「新账户」 |
| 密钥 | 等宽字体输入 + 密钥图标 | 自动忽略空格与连字符；Base32 字符集校验 |
| 高级选项 | 可折叠卡：算法 SHA-1 / 位数 6 位 / 刷新周期 30 秒 | 默认值即可用 |
| 实时预览 | 高 76 卡片：验证码 + 倒计时环 + 「实时预览 · 剩余 N 秒」 | 密钥变更即刷新 |
| 提交 | 主按钮「添加账户」，高 44，强调色底 | 成功后回列表、新账户置顶、toast「已添加「名称」」 |

### 7.5 FR-05 账户详情（P0）

| 区块 | 规格 |
|---|---|
| 身份区 | 64 × 64 圆角 18 渐变图标（带投影）+ 账户名 20 px + 账户标识 13 px |
| 验证码主视觉 | 168 × 168 倒计时大环（描边 5，轨道 `#3A3F45`，进度强调色）内嵌 28 px 等宽验证码 + 「N 秒后刷新」 |
| 参数卡 | 账户类型（TOTP）/ 哈希算法 / 位数 / 刷新周期 / 添加时间 |
| 操作区 | 「复制验证码」主按钮（占满剩余宽度）+ 「删除」描边按钮（宽 100，红字红描边） |
| 标题栏右侧 | 编辑（铅笔）+ 删除（垃圾桶，红色）图标 |

### 7.6 FR-06 删除（P0）

- 弹窗覆盖详情页：全屏遮罩（`rgba(13,13,15,.65)`）+ 居中弹窗（304 宽，圆角 16）；
- 文案必须明示不可恢复：「删除后需要重新扫描二维码或输入密钥才能恢复…」；
- 「取消」留在详情页；「删除」（红底 `#E5484D`）执行后：回列表、列表同步减少、toast「已删除「名称」」；
- **不允许**在无确认的情况下直接删除。

### 7.7 FR-07 TOTP 引擎与倒计时（P0）

| 编号 | 需求 | 规格 |
|---|---|---|
| T1 | 算法 | RFC 6238，HMAC-SHA-1，6 位，30 秒周期（高级选项可配置） |
| T2 | 显示格式 | `XXX XXX`（3 + 3，中间空格）；复制时去除空格 |
| T3 | 倒计时环 | 进度 = 剩余秒数 / 30；连续平滑更新（非整秒跳变） |
| T4 | 告警态 | 剩余 ≤ 5 秒：环进度变 `#F28B82`，轨道变 `#3A2B2B`；多行独立判定 |
| T5 | 刷新 | 跨周期时所有可见验证码同步刷新，无闪烁 |
| T6 | 一致性 | 与标准 TOTP 实现（如 Google Authenticator）逐秒一致 |

---

## 8. 视觉规范（Design Tokens）

### 8.1 颜色

| Token | 值 | 用途 |
|---|---|---|
| `--win-bg` | `#1B1C1E` | 窗口底 / 行默认底 |
| `--titlebar` | `#1F2124` | 标题栏 |
| `--divider` | `#26282C` | 分隔线 |
| `--surface` | `#232528` | 悬停态 / 搜索框 / 次级按钮 |
| `--card` | `#1F2225` | 卡片（描边 `#292E30`） |
| `--input` | `#212326` | 输入框（描边 `#2E3036`） |
| `--seg` / `--seg-active` | `#26282C` / `#43474B` | 分段控件 |
| `--t1` / `--t2` / `--t3` / `--t4` | `#E9EBED` / `#8A8F94` / `#63686D` / `#7C8186` | 文字四级 |
| `--code` | `#F2F4F5` | 验证码数字 |
| `--accent` / `--on-accent` | `#8AB4F8` / `#1B1C1E` | 强调色及其上的文字 |
| `--danger` / `--danger-t` | `#E5484D` / `#F28B82` | 危险（实心 / 文字） |
| `--ring-track` / 告警轨道 | `#33363A` / `#3A2B2B` | 倒计时环轨道（详情大环为 `#3A3F45`） |
| 复制反馈 | `#2E3237` | 行复制后短暂底色 |

### 8.2 字体与字号

| 用途 | 字体 | 字号/字重 | 备注 |
|---|---|---|---|
| 窗口标题 | Noto Sans SC | 13 / Medium | |
| 账户名（列表） | Noto Sans SC | 15 / SemiBold | |
| 验证码（列表） | JetBrains Mono | 22 / Medium | 字间距 1.5 px |
| 验证码（详情） | JetBrains Mono | 28 / Medium | 字间距 1 px |
| 验证码（预览） | JetBrains Mono | 20 / Medium | 字间距 1.5 px |
| 正文/标签 | Noto Sans SC | 11–15 / Regular–Medium | 见各组件规格 |

### 8.3 圆角与尺寸速查

| 元素 | 值 |
|---|---|
| 窗口 12 · 卡片/主按钮 12 · 输入框/操作块/弹窗按钮 10 · 分段容器 9（段 7）· 头像 18 · 胶囊按钮 20 · 弹窗 16 | |
| 标题栏 52 · 行 76 · 底部栏 72 · 搜索框 36 · 输入框 42 · 主按钮 44 · 预览卡 76 · 操作块 72×76 | |
| 页面内边距 20 · 搜索区 16 · 列表左右 8/上下 4 | |

---

## 9. 边界与异常处理

| # | 场景 | 处理 |
|---|---|---|
| E1 | 剪贴板不可用 / 权限被拒 | toast「复制失败，请手动复制」，不静默失败 |
| E2 | 删除至 0 个账户 | 列表空、计数「共 0 个账户」；详情页入口自然消失 |
| E3 | 密钥含空格/连字符/小写 | 自动清洗（去空白与连字符、转大写）后参与计算 |
| E4 | 密钥非法（非 Base32） | 提交时校验并提示，不产生错误验证码 |
| E5 | 系统时间不准 | 验证码与服务器实际值可能偏差；提示用户校时（P1：时间偏移显示） |
| E6 | 剩余 ≤ 5 秒时复制 | 照常复制当前值；toast 正常（不做二次确认，避免打断） |
| E7 | 搜索无结果 | 空列表 + 计数归零，不清空用户输入 |
| E8 | 拖拽中切换屏幕 / 最小化 | 行状态复位，无残留位移 |

---

## 10. 非功能需求

### 10.1 安全（本产品为硬性要求）

| 编号 | 需求 | 优先级 |
|---|---|---|
| S1 | 密钥仅存于 macOS Keychain，禁止明文落盘 / 进日志 | P0 |
| S2 | 验证码不写入任何日志、崩溃报告、截图缓存 | P0 |
| S3 | 进程信息不泄露密钥（命令行参数 / `ps` 可见性控制） | P0 |
| S4 | 复制到剪贴板的验证码支持 N 秒后自动清除（默认 30 s） | P1 |
| S5 | 应用锁定（Touch ID / 密码），隐藏窗口内容于任务切换器 | P1 |

### 10.2 性能

| 编号 | 需求 | 指标 |
|---|---|---|
| P-1 | 倒计时连续更新 | 单实例 ≤ 7 个环同时动画，CPU 占用可忽略（< 1%） |
| P-2 | 点按复制响应 | < 100 ms 内给出视觉反馈 |
| P-3 | 左滑跟手 | 位移与指针位移 1:1，无可感知延迟 |

### 10.3 兼容与可访问性

| 编号 | 需求 |
|---|---|
| C1 | 鼠标拖拽与触屏/触控板手势均可用（触控板横滑为 P1） |
| C2 | 对比度满足 WCAG AA（正文 ≥ 4.5:1，深色底上最低 `--t3` 仅用于辅助信息） |
| C3 | 行为可键盘触达（P1：Tab 聚焦行 + Enter 复制） |

---

## 11. 验收清单（汇总）

| 模块 | 验收项 |
|---|---|
| 列表 | 行规格逐像素对齐；验证码等宽不跳动；滚动正常 |
| 行交互 | 点按复制 1 步完成；左滑呼出；吸附/回弹阈值正确；拖拽后 click 不误触；同时仅一行展开；右滑不错位；复制失败有提示 |
| 搜索 | 实时过滤、字段覆盖、计数同步 |
| 添加 | 分段切换互斥；密钥清洗；实时预览随密钥刷新；添加后置顶 |
| 详情 | 参数卡与账户一致；大环/字号/轨道色正确 |
| 删除 | 必经二次确认；确认后列表同步减少并回列表 |
| TOTP | 与标准实现逐秒一致；告警态 ≤ 5 s 生效 |
| 全局 | 400×700 等比缩放；Esc/←/→ 行为正确 |

---

## 12. 后续迭代 Backlog

| 项 | 说明 | 优先级 |
|---|---|---|
| 触控板横滑 | macOS 双指横滑是 wheel 事件，需单独接入手势（当前仅鼠标拖拽/触屏） | P1 |
| 全局快捷键 | 一键呼出主窗口 / 直接搜索 | P1 |
| 编辑账户 | 名称、密钥、高级参数可改 | P1 |
| 导入导出 | 加密备份文件 / 从其他验证器迁移 | P1 |
| 菜单栏常驻 | 取码不切窗口 | P2 |
| 同步与锁定 | iCloud 同步、Touch ID 锁定 | P2 |

---

## 13. 附录

- **可交互 Demo**：`mfa-authenticator/index.html`（单文件，双击打开；内置真实 RFC 6238 TOTP，验证码为实时计算值，非截图）
- **设计稿**：Ardot 画布 5 屏（列表 / 扫码 / 手动 / 详情 / 删除确认）
- **已知差异**：Demo 中扫描线为往复动画（设计稿为静止帧）；验证码数字为实时计算，与设计稿示意数字不同
